#+PROPERTY: header-args :dir ..
#+PROPERTY: header-args:sqlite :db data/datasets/rhyme.db :colnames yes

#+begin_src sqlite
  SELECT COUNT(*) FROM rhyme_annotations
#+end_src

#+RESULTS:
| COUNT(*) |
|----------|
|   747521 |


#+begin_src sqlite
  DROP VIEW IF EXISTS validated_rhyme_groups
#+end_src

#+RESULTS:

#+begin_src sqlite
  CREATE VIEW validated_rhyme_groups AS
  WITH text_avg_coef AS (
      SELECT poem_id, AVG(rhyming_coef) as avg_coef FROM rhyme_annotations
      GROUP BY(poem_id)
  ),
  rounded AS (
      SELECT
          ra.poem_id,
          seq,
          rhyme_group,
          ROUND(ra.rhyming_coef, 2) AS rhyming_coef,
          ROUND(tac.avg_coef, 2) AS avg_coef
      FROM rhyme_annotations ra
      JOIN text_avg_coef tac ON ra.poem_id = tac.poem_id
  )
  SELECT *
  FROM rounded
  WHERE
      rhyming_coef >= 0.2
      OR (rhyming_coef >= 0.1 AND avg_coef >= 0.45)
#+end_src

#+RESULTS:

#+begin_src sqlite
  SELECT COUNT(*) FROM validated_rhyme_groups
#+end_src

#+RESULTS:
| COUNT(*) |
|----------|
|   737659 |

#+begin_src sqlite
  SELECT vrg.rhyming_coef,
         json_group_array(DISTINCT r.word) AS words
  FROM validated_rhyme_groups vrg
  JOIN rhymes r
     ON vrg.poem_id     = r.poem_id
     AND vrg.seq         = r.seq
     AND vrg.rhyme_group = r.rhyme_group
  WHERE avg_coef < 0.45
  GROUP BY r.poem_id, r.seq, r.rhyme_group
  ORDER BY rhyming_coef ASC
  LIMIT 20
#+end_src

#+RESULTS:
| rhyming_coef | words       |                |
|--------------+-------------+----------------|
|          0.2 | ["долг"     | вздох]         |
|          0.2 | ["ударяет"  | леты]          |
|          0.2 | ["суетится" | простотою]     |
|          0.2 | ["затворы"  | нивы]          |
|          0.2 | ["красы"    | эреба]         |
|          0.2 | ["судьбою"  | лиру]          |
|          0.2 | ["судья"    | твоя]          |
|          0.2 | ["зови"     | одиноких]      |
|          0.2 | ["полет"    | дышит]         |
|          0.2 | ["потонуло" | возвещают]     |
|          0.2 | ["мольбою"  | летят]         |
|          0.2 | ["мая"      | падет]         |
|          0.2 | ["твою"     | пташечку-жену] |
|          0.2 | ["право"    | велико]        |
|          0.2 | ["осуждено" | поэт]          |
|          0.2 | ["забвенью" | шумно]         |
|          0.2 | ["мечты"    | делили]        |
|          0.2 | ["поэт"     | тоски]         |
|          0.2 | ["доход"    | обедняли]      |
|          0.2 | ["купил"    | забывал]       |

#+begin_src sqlite
  SELECT p.path,
         vrg.rhyming_coef,
         vrg.avg_coef,
         json_group_array(DISTINCT r.word) AS words
  FROM validated_rhyme_groups vrg
  JOIN rhymes r
     ON vrg.poem_id     = r.poem_id
     AND vrg.seq         = r.seq
     AND vrg.rhyme_group = r.rhyme_group
  JOIN poems p ON vrg.poem_id = p.ROWID
  GROUP BY r.poem_id, r.seq, r.rhyme_group
  ORDER BY rhyming_coef ASC
  LIMIT 10
#+end_src

#+RESULTS:
| path                          | rhyming_coef | avg_coef | words        |              |
|-------------------------------+--------------+----------+--------------+--------------|
| xix/1790-1810_poets/poets-140 |          0.1 |     0.55 | ["герой"     | паче]        |
| xix/1830/1830-424             |          0.1 |      0.5 | ["живые"     | счастливый]  |
| xix/1860/1860-101             |          0.1 |     0.51 | ["вздох"     | жег]         |
| xix/apuhtin/apuh-266          |          0.1 |     0.53 | ["пока"      | обоих к]     |
| xix/batjushkov/bat-062-       |          0.1 |     0.55 | ["закрывают" | добродетель] |
| xix/batjushkov/bat-147-       |          0.1 |     0.45 | ["руке"      | седой]       |
| xix/benediktov/bene-011-4     |          0.1 |      0.5 | ["зажег"     | вздох]       |
| xix/ershov/ersh-021           |          0.1 |     0.46 | ["судьбу"    | петра]       |
| xix/fet/fet-765               |          0.1 |     0.59 | ["свое"      | монтерё]     |
| xix/mej/mej-104               |          0.1 |     0.51 | ["освети"    | ложе]        |

#+begin_src sqlite
  WITH text_avg_coef AS (
  SELECT poem_id, AVG(rhyming_coef) as avg_coef FROM rhyme_annotations
  GROUP BY(poem_id)
  )
  SELECT printf('%.1f-%.1f',
                 CAST(avg_coef * 10 AS INT) / 10.0,
                 (CAST(avg_coef * 10 AS INT) + 1) / 10.0)
  AS strata,
  COUNT(*) AS n,
  ROUND(100.0 * COUNT(*) / (SELECT COUNT(*) FROM text_avg_coef), 2) AS pct
  FROM text_avg_coef
  GROUP BY strata
  ORDER BY MIN(avg_coef)
#+end_src

#+RESULTS:
|  strata |     n |   pct |
|---------+-------+-------|
| 0.0-0.1 |    53 |  0.08 |
| 0.1-0.2 |   147 |  0.22 |
| 0.2-0.3 |   290 |  0.43 |
| 0.3-0.4 |  1274 |   1.9 |
| 0.4-0.5 |  8468 |  12.6 |
| 0.5-0.6 | 32601 | 48.52 |
| 0.6-0.7 | 20444 | 30.43 |
| 0.7-0.8 |  3412 |  5.08 |
| 0.8-0.9 |   426 |  0.63 |
| 0.9-1.0 |    30 |  0.04 |
| 1.0-1.1 |    49 |  0.07 |

#+begin_src sqlite
  SELECT
    CASE
      WHEN rhyming_coef = 0.0 THEN '0.0'
      WHEN rhyming_coef = 1.0 THEN '1.0'
      ELSE printf('%.1f-%.1f',
                  CAST(rhyming_coef * 10 AS INT) / 10.0,
                  (CAST(rhyming_coef * 10 AS INT) + 1) / 10.0)
    END AS strata,
    COUNT(*) AS n,
    ROUND(100.0 * COUNT(*) / (SELECT COUNT(*) FROM rhyme_annotations), 2) AS pct
  FROM rhyme_annotations
  GROUP BY strata
  ORDER BY MIN(rhyming_coef)
#+end_src

#+RESULTS:
|  strata |      n |   pct |
|---------+--------+-------|
|     0.0 |   5934 |  0.79 |
| 0.0-0.1 |   1240 |  0.17 |
| 0.1-0.2 |  10736 |  1.44 |
| 0.2-0.3 |  19889 |  2.66 |
| 0.3-0.4 |  55127 |  7.37 |
| 0.4-0.5 |  99278 | 13.28 |
| 0.5-0.6 | 194201 | 25.98 |
| 0.6-0.7 | 191600 | 25.63 |
| 0.7-0.8 |  87505 | 11.71 |
| 0.8-0.9 |  58004 |  7.76 |
| 0.9-1.0 |   2219 |   0.3 |
|     1.0 |  21788 |  2.91 |

#+begin_src sqlite
  DROP VIEW IF EXISTS excluded_rhymes
#+end_src

#+RESULTS:

#+begin_src sqlite
  CREATE VIEW excluded_rhymes AS
  WITH text_avg_coef AS (
      SELECT poem_id, AVG(rhyming_coef) AS avg_coef
      FROM rhyme_annotations
      GROUP BY poem_id
  )
  SELECT authors.name AS author,
         poems.path AS poem_path,
         poems.header AS title,
         json_group_array(DISTINCT r.word) AS words,
         ROUND(AVG(ra.rhyming_coef), 2) AS rhyming_coef,
         ROUND(tac.avg_coef, 2)         AS avg_coef
  FROM rhymes r
  JOIN poems ON r.poem_id = poems.ROWID
  JOIN authors ON poems.author_id = authors.ROWID
  JOIN rhyme_annotations ra
      ON ra.poem_id     = r.poem_id
     AND ra.seq         = r.seq
     AND ra.rhyme_group = r.rhyme_group
  JOIN text_avg_coef tac ON tac.poem_id = r.poem_id
  WHERE NOT EXISTS (
      SELECT 1
      FROM validated_rhyme_groups vrg
      WHERE vrg.poem_id     = r.poem_id
        AND vrg.seq         = r.seq
        AND vrg.rhyme_group = r.rhyme_group
  )
  GROUP BY r.poem_id, r.seq, r.rhyme_group
#+end_src

#+RESULTS:

#+begin_src sqlite
  SELECT poem_path, words, rhyming_coef, avg_coef FROM excluded_rhymes WHERE rhyming_coef > 0.1 ORDER BY avg_coef DESC, rhyming_coef DESC LIMIT 10
#+end_src

#+RESULTS:
| poem_path                | words          | rhyming_coef | avg_coef |      |
|--------------------------+----------------+--------------+----------+------|
| xix/polezhaev/pole-086-1 | ["груди"       | расцвести]   |     0.19 | 0.44 |
| xx/blok/blok2-1032       | ["ацетилен"    | господин]    |     0.19 | 0.44 |
| xx/ehrehnburg/eren-477   | ["медвежонка"  | потемки]     |     0.19 | 0.44 |
| xx/ehrehnburg/eren-570   | ["хлопьями"    | потопленной] |     0.19 | 0.44 |
| xx/cvetaeva/cvet2-286    | ["тела"        | скромной]    |     0.19 | 0.44 |
| xx/cvetaeva/cvet2-331    | ["приложим"    | сережа]      |     0.19 | 0.44 |
| xx/samoilov/samoi-362    | ["полстолетье" | порою]       |     0.19 | 0.44 |
| xx/semenov/semen-018     | ["меня"        | бесплотный]  |     0.19 | 0.44 |
| xx/mironov/miron-206     | ["невдомек"    | голубок]     |     0.19 | 0.44 |
| xx/sosnora/sosnor-382    | ["публиковать" | потолка]     |     0.19 | 0.44 |

#+begin_src sqlite
  SELECT poem_path, words, rhyming_coef, avg_coef FROM excluded_rhymes ORDER BY rhyming_coef DESC LIMIT 10
#+end_src

#+RESULTS:
| poem_path               | words        | rhyming_coef    | avg_coef |      |
|-------------------------+--------------+-----------------+----------+------|
| xix/1840/1840-089       | ["понеслась" | седая]          |     0.19 | 0.23 |
| xix/1840/1840-089       | ["воспевать" | оговорок]       |     0.19 | 0.23 |
| xix/1840/1840-089       | ["заключил"  | богатой]        |     0.19 | 0.23 |
| xix/1840/1840-251       | ["миронтень" | ничего]         |     0.19 | 0.17 |
| xix/1840/1840-265       | ["хвоста-то" | неладно]        |     0.19 | 0.24 |
| xix/baratynsky/br1-271  | ["катона"    | преданий]       |     0.19 | 0.35 |
| xix/batjushkov/bat-093- | ["страстном" | съединен]       |     0.19 | 0.34 |
| xix/buturlin/butur-042  | ["молодой"   | матерью-землей] |     0.19 | 0.39 |
| xix/iskra/iskra-78      | ["обедней"   | встретил]       |     0.19 | 0.29 |
| xix/iskra/iskra-79      | ["когда-то"  | медитаций]      |     0.19 | 0.23 |

#+begin_src sh
  sqlite3 -header -csv data/datasets/rhyme.db "SELECT * FROM excluded_rhymes" > data/excluded_rhymes.csv
#+end_src

#+RESULTS:
